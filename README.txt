Local Setup:
- change the links in style.css and index.html so that it paths to proper folder
	/css/styles.css -> css/styles.css
	/images/filname.png -> ../images/filename.png

Online Setup:
- change the path name such that it refers to the base folder (where the index.html is located)
	/css/styles.css
	/images/filname.png